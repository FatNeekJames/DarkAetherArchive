import { zombieMaps } from '../../data/maps';
export default async function MapDossier({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const map=zombieMaps.find(([id])=>id===slug);const name=map?.[1]??'Map dossier';const game=map?.[2]??'Zombies';return <main className="route-page map-dossier"><header><a href="/">DA / DARK AETHER ARCHIVE</a><nav><a className="selected" href="/maps">Maps</a><a href="/intel">Intel</a><a href="/timeline">Timeline</a><a href="/signals">Signals</a></nav></header><section className="map-dossier-hero"><a href="/maps">← MAP INDEX</a><span>{game.toUpperCase()} // OPERATIONAL DOSSIER</span><h1>{name}</h1><p>Browse all indexed collectibles, story evidence, and connected lore for this operation.</p></section><section className="dossier-content"><p className="section-kicker">INTELLIGENCE DATABASE</p><h2>Map records</h2><p className="section-intro">This dossier is ready for all source-tracker entries assigned to {name}. Each imported entry receives its own classified record, collection location, and source reference.</p><a className="dossier-cta" href="/intel">OPEN INTEL ARCHIVE →</a></section></main>}
